{
  "buildCommand": "pip install --break-system-packages -r requirements.txt",
  "devCommand": "uvicorn api.main:app --reload",
  "installCommand": "pip install --break-system-packages -r requirements.txt",
  "framework": null,
  "functions": {
    "api/main.py": {
      "maxDuration": 30,
      "memory": 1024
    }
  },
  "regions": ["iad1"],
  "outputDirectory": "api"
}